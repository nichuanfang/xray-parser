{
  "domainStrategy": "IPIfNonMatch",
  "rules": [
    {
        "type": "field",
        "outboundTag": "direct",
        "protocol": ["bittorrent"]
    },
    {
        "type": "field",
        "outboundTag": "dns-out",
        "inboundTag": ["socks-in", "http-in"],
        "network": "udp",
        "port": 53
    },
    {
        "type": "field",
        "outboundTag": "block",
        "domain": ["geosite:category-ads-all"]
    }
  ]
}
